# 具身智能：面经导向学习总路线

课程基线：v1.0（2026-09-15）。仓库执行修订：v1.4（2026-09-18）。

**保留原来的 72 个知识单元、95 道面经及 P0–P3 项目范围；v1.4 不增加知识单元，而是在 v1.3 必要工程关卡基础上，重构 G01–G06，把零基础数据结构、C++ 面经点和 Python 工程基础明确穿插到 A–H 主线。** 动态状态以 [LEARNING_STATE.md](LEARNING_STATE.md) 为准，问题映射以 [INTERVIEW_MAP.md](INTERVIEW_MAP.md) 为准。

## 1. 目标与边界

目标：建立具身智能算法实习所需的机器人学、仿真、视觉抓取、模仿学习、强化学习与 VLA 能力；每项面试回答由公式、代码或实验支撑。

设备安排沿用已有 Windows 本机与 Linux 远程训练环境；不将新电脑、真实机械臂或相机采购作为入门前提。现有成功环境不无故升级、重装。真实硬件经验与仿真、公开数据、合成验证分开表述。

主线：**二连杆与基础控制 → UR5 系列/规划/控制 → 相机与传统视觉抓取 → 数据与模仿学习 → SAC → VLA/π 系列 → 面试验收。**

72 个单元是稳定的能力索引，不是 72 条长回复。一个单元可拆成多个小课；每次只有一个主问题、1–3 个核心新概念。拆分或交叉覆盖必须有明确的执行顺序，不能跳过未完成的验收。

## 2. 必要工程关卡怎样接入 72 单元主线

72 个知识单元负责“学什么”，P0–P3 负责“把知识真正串起来做什么”。**必要工程不是可选加餐，也不等到 H 阶段才做**。达到规定插入点后，先完成对应工程关卡，再进入下一阶段；若环境阻塞，必须记录真实阻塞点和替代验证，不把 RViz 动画、直接改 qpos、伪造日志当成项目完成。

工程关卡不新增 A–H 编号，也不改变 72 单元总数；它们是阶段之间的必做验收。

| 插入位置 | 工程关卡 | 必做内容 | 通过后进入 |
|---|---|---|---|
| A08 后 | **P0：二连杆基础练习台验收** | FK/IK/Jacobian/奇异性/PD 与扰动证据整理；补齐最小可复现代码和说明 | B01 |
| B10 后、C01 前 | **P1-A：已知目标位姿机械臂闭环** | 冻结 UR5/UR5e + 夹爪；MoveIt 规划；读取 RobotTrajectory；显式映射到 MuJoCo；接近→闭合→抬起 | C01 |
| C05–C07 | **P1-B：传统视觉抓取完整闭环** | 相机/深度/点云→坐标变换→GraspNet 候选→筛选→MoveIt→MuJoCo 抓取；固定场景评估 | C08 / D01 |
| D03 后 | **P2-A：BC 最小模仿学习闭环** | 读取/回放示范→定义 observation/action→训练小 BC→闭环执行→独立成功判据 | D04 |
| D07 后 | **P2-B：ACT / Diffusion Policy 同任务对照** | 同一数据、同一任务完成 ACT 与小型 Diffusion Policy 训练/推理/扰动评估，不只看 loss | D08 / E01 |
| E14–E16 | **P3：SAC 控制与失效分析** | 从 P0 低维任务开始，完成 replay/actor/critic/alpha/target 更新与评估；至少两项可解释对照 | F01 |
| F04–F12 | **P2-C：VLA 扩展验收** | 在已工作的任务/数据链上做公开 checkpoint 前向与任务评估；条件允许再做小规模 LoRA；未公开权重只读机制 | H01 |
| H01–H04 | **Final：项目答辩与材料化** | P1/P2/P3 统一 README、视频、指标、失败边界、个人修改；Q001–Q095 模拟面试 | 完成首轮主线 |

### G 阶段穿插位置（零基础 C++ / 数据结构 / Python）

G 阶段不作为一整块拖到最后，而是按“学到哪里就补哪里”的方式穿插。用户默认**没有系统学过数据结构**，因此首次出现复杂度、容器、heap、hash、tree、graph 等概念时必须按零基础教学。

| 插入位置 | G 单元 | 为什么放这里 |
|---|---|---|
| **P1-A 完成后、C01 前** | **G01 + G02** | 先补 C++ 最基础语法/对象/引用，以及数组、vector、链表、栈、队列、复杂度；为后续看 ROS/MoveIt/视觉工程源码打底。 |
| **C04 后、C05 GraspNet 前** | **G03** | 补 hash/map/set、heap/priority_queue、树/图、BFS/DFS；和候选筛选、A*/RRT、图搜索直接关联。 |
| **D02 后、D03 BC 前** | **G04** | Python 赋值/浅拷贝/深拷贝、容器与 NumPy view/copy；防止数据集、batch、episode 被共享引用坑到。 |
| **E08 后、E09 Actor-Critic 前** | **G05** | C++ 面经强化：虚函数/纯虚函数、RAII、unique/shared/weak_ptr；同时理解工程接口/所有权。 |
| **A–H 全程穿插，H 前集中验收** | **G06** | 装饰器、计时、Git、配置、日志、断言、测试、种子、可复现工程；直接服务每个项目。 |

原则：**P1-A 仍然先做，不因数据结构补课再次推迟第一个机械臂工程。** P1-A 完成后，正式补 G01/G02，再进入 C01。

### 当前执行原则

- **P0** 是基础证据工程，不要求购买真实硬件。
- **P1-A** 是第一个真正的完整机械臂工程；首版故意不加相机和 GraspNet，先打通“目标位姿→规划→轨迹→物理执行→抓取”。
- **P1-B** 再加入视觉与 GraspNet，形成传统视觉抓取完整闭环。
- **P2-A/P2-B/P2-C** 都尽量复用已经跑通的机器人任务和数据接口，避免每换一种算法就重搭一套环境。
- **P3** 先在低维、可解释任务上完成 SAC，再讨论复杂抓取或 VLA+RL。
- **H 阶段不是第一次做项目**，而是把此前真实完成的项目整理成可答辩、可追问、可复现的面试材料。

## 3. 时间预算

参考预算：**17 周 × 每周 6 天 × 每天 3 小时 = 306 小时**。这是学习安排，不是完成、就业或掌握程度的保证。已学会的内容经确认后跳过；理解回补和调试可顺延，不靠省略前提追日历。

每日参考：45 分钟概念/推导，100 分钟实验与修改，35 分钟解释、复盘与记录。GPU 后台训练的等待不等于有效学习时间。G 阶段穿插，其时间已包含于各阶段，不额外开长线。

| 阶段 | 单元数 | 预算 | 项目 |
|---|---:|---|---|
| A 二连杆与 MuJoCo 基础闭环 | 8 | 第 1–2 周，36 小时 | P0 |
| B UR5 系列、规划与控制 | 10 | 第 3–5 周，54 小时 | P1-A 前置知识；B10 后完成 P1-A |
| C 相机、标定、GraspNet、仿真差距 | 8 | 第 6–7 周，36 小时 | P1-B 完整视觉抓取闭环 |
| D 数据与模仿学习 | 8 | 第 8–9 周，36 小时 | P2-A / P2-B |
| E 强化学习与 SAC 深入 | 16 | 第 10–13 周，72 小时 | P3 必做工程 |
| F VLA 与 π 系列 | 12 | 第 14–16 周，54 小时 | P2-C VLA 扩展 |
| G C++ / Python | 6 | 穿插 A–F，已计入预算 | 当前项目 |
| H 项目整合与面试 | 4 | 第 17 周，18 小时 | 最终验收 |

## 4. 项目与验收

### P0：二连杆基础练习台

保留现有单杆模型和入口；先静态几何，再物理推进，再执行器，扩成二连杆。不得把完整新工程覆盖到学生尚未理解的代码上。

交付：实际 XML/Python、模型坐标说明、环境版本、FK 数值对照、IK 回代、奇异性方向实验、跟踪与扰动曲线。

验收：至少三组非退化关节角和一组伸直状态；先预测末端位置再运行。解释 body 原点、joint pos/axis、末端取点、长度/角度单位、参考值和默认值继承。明确精度阈值，不以画面相似判通过。

### P1：传统视觉抓取闭环

P1 明确拆成两个必做工程关卡，但仍属于同一个 P1 项目。

**P1-A（B10 后、C01 前）——已知目标位姿机械臂闭环：**
冻结一种 UR5 系列具体型号和一种夹爪，模型、几何、关节限位和工具坐标统一。UR5 与 UR5e 不混用参数。完成：模型/夹爪安装 → 已知目标抓取 Pose → MoveIt IK/碰撞/规划 → 读取 RobotTrajectory → MuJoCo 物理执行 → 夹爪闭合 → 抬起。首版不要求相机和 GraspNet，重点是第一次打通真正机械臂端到端工程。

**P1-B（C05–C07）——传统视觉抓取完整闭环：**
在 P1-A 已工作的机械执行链上增加：相机/深度/点云 → 标定与相机到基座变换 → GraspNet 候选 → 分数/宽度/碰撞/IK/路径筛选 → MoveIt → MuJoCo 抓取 → 固定场景批量评估。

MoveIt 与 MuJoCo 的连接是显式集成任务，不是两个库装上就自动互通。首版允许导出轨迹再回放；关节名、顺序、单位、时间、控制方式逐项核对。RViz 动画、模拟硬件显示或直接改 qpos 不等于完成物理抓取。

GraspNet 首版用预训练权重与少量官方样例，不从头训练。环境阻塞时可暂用保存的候选验证下游，但需记录并补回推理，不默默替换算法。

交付：安装坐标说明、候选保留/拒绝理由、规划轨迹、录像、固定测试集成功率与失败分类。建议先用 20 个固定场景/种子做功能验收，不把这个规模包装成充分统计证据。

### P2：模仿学习与 VLA 扩展

用一个小任务和一组明确划分的数据。优先沿用已工作的机器人任务；为框架兼容改任务时记原因与保留的验收，不无限移植。

次序：示范读取/回放 → 观测动作检查 → 小 BC → ACT → 同任务小型 Diffusion Policy 对照 → 扰动/未见场景评估。

LeRobot 是数据和训练主线，作者 ACT/Diffusion 仓库用于对照，不同时维护许多训练系统。按 episode/场景划分训练测试，不把相邻帧随机拆开制造虚假的泛化。

交付：数据卡、划分、训练曲线、独立成功率、动作频率/延迟、中途扰动结果、一次单因素对照。VLA 扩展先做小模型 LoRA，再验证公开 checkpoint 的实际输入输出和任务评估。随机输入的前向运行不等于任务评估；未公开权重的版本只读机制，不设不可执行的复现目标。

### P3：SAC 控制与失效分析

从 P0 二连杆低维到达任务开始，不直接上多相机、复杂接触、稀疏奖励抓取。先验证环境和可靠基线，再逐步替换成能解释的教学实现。

交付：环境接口、成功判据、动作边界、Replay Buffer、Actor/Critic/Alpha 更新、目标网络、评估脚本和种子。记录回报、成功率、Q、Alpha、动作幅度。

优先两项对照：固定/自适应 Alpha；有/无延迟或随机化。核心小实验用多个随机种子并报告波动。PER、混合动作、离线 RL 首轮以原理与小实验为主，不把所有变体强塞进主 SAC。

## 5. 完整 72 个知识单元

### A｜二连杆与 MuJoCo 基础闭环

| 编号 | 知识内容 | 实验与验收证据 |
|---|---|---|
| A01 | 环境与已有进度核验：Python/MuJoCo、解释器、环境隔离、路径与版本；不默认重装。 | 沿用成功入口，保存版本与运行信息；原始错误原样记录。 |
| A02 | 刚体、形状与坐标系：body/geom、父子坐标、原点选择；关节间距与 capsule 外轮廓长度不同。 | 单独改变 body pos 与 geom，预测再观察；说明杆端坐标系与近端关节坐标系的区别。 |
| A03 | 关节位置、方向与默认值：joint pos/axis、相对父体自由度、单位、参考值、零位姿与 default 继承。 | 移动关节锚点，改变旋转轴；解释内置值与实际继承值。 |
| A04 | 仿真状态与 FK：MjModel/MjData、qpos/qvel、计算当前与推进时间；自己的正运动学。 | 状态读取和重力实验；二连杆 FK 与仿真末端坐标数值对照。 |
| A05 | 驱动命令与直接改状态：motor/position、ctrl 的意义取决于执行器。 | 同一目标比较直接设角度与驱动过程；不把 ctrl 一律当角度或力矩。 |
| A06 | 二连杆 IK：位置任务与位姿任务、解析两分支、可达范围、数值迭代与初值。 | 多点 IK 回代 FK；不可达返回明确信息；比较不同初值。 |
| A07 | Jacobian、奇异性与阻尼：速度/增量映射、任务与矩阵维度、伪逆及局限。 | 对照弯曲/近伸直/伸直的指定方向速度；不是所有任务都失控。 |
| A08 | 最小控制与验收：PD 直觉、步长/控制周期、限位、记录与扰动。 | 目标/实际角度曲线、末端误差、一次扰动恢复；独立修改并解释。 |

### B｜UR5 系列、规划与控制

| 编号 | 知识内容 | 实验与验收证据 |
|---|---|---|
| B01 | URDF：link/joint、固定 origin 与关节运动、visual/collision/inertial。 | 读一条运动链，标注原点、轴、单位、类型。 |
| B02 | 三维资产与夹爪：STL、mesh、纯色/材质/纹理、安装变换与工具坐标。 | 增加固定安装，检查尺度、朝向、碰撞、质量；冻结机器人型号。 |
| B03 | ROS 2 与坐标传递：节点、话题、动作接口、TF 分开讲。 | 追踪一条命令与反馈，读取关节状态和工具变换。 |
| B04 | UR5 逆解与奇异处理：解析/数值/优化、限位、碰撞、连续性和代价。 | 比较多组逆解，记录拒绝原因；没有脱离任务的唯一最优解。 |
| B05 | Dijkstra/A*：Open/Closed、已走代价、启发函数、最优性条件。 | 手写二维网格搜索，比较节点展开与路径代价。 |
| B06 | RRT 与配置空间：采样、最近节点、扩展、边碰撞；路径与轨迹不同。 | 先二维障碍演示，再理解关节空间规划；不能只检查端点。 |
| B07 | MoveIt 规划到执行：位姿、IK、碰撞、路径、时间参数化与控制器。 | 读取 RobotTrajectory，按名字/单位/时序在 MuJoCo 回放。 |
| B08 | PD/PID：比例、积分、微分、超调、稳态误差、噪声与积分饱和。 | 一次改一个增益；加入恒定负载对照，不盲调。 |
| B09 | MPC：预测、约束优化、只执行首步、滚动求解。 | 一维或单关节近似系统的约束跟踪，不要求工业级非线性 MPC。 |
| B10 | 延迟诊断：计算、通信、排队、控制频率、滤波、限速与机械响应。 | 链路时间戳、人工延迟注入、排查树与优化前后对照。 |

### C｜相机、标定、GraspNet 与仿真差距

| 编号 | 知识内容 | 实验与验收证据 |
|---|---|---|
| C01 | 图像与内参：像素、相机坐标、焦距、主点、针孔假设。 | 三维点投影，检查单位与轴方向。 |
| C02 | 深度与点云：深度尺度、缺失值、有效区域、点云/法向。 | 从深度还原点云，检查米/毫米混淆。 |
| C03 | 内外参标定：成像与坐标关系、畸变、重投影误差。 | 公开或自采图像的标定，说明来源与误差。 |
| C04 | 手眼标定：眼在手上/固定相机、变换链、AX=XB 前提。 | 合成已知变换验证；没有真机不能声称真机标定。 |
| C05 | GraspNet 输入输出：点云、位置、朝向、宽度、分数和夹爪轴约定。 | 预训练模型处理样例，逐字段可视化。 |
| C06 | 候选筛选：分数、宽度、碰撞、重复抑制、IK、路径与接近方向。 | 每个候选有保留/拒绝理由；与只按分数对照。 |
| C07 | 抓取集成：相机到基座、工具安装、接近/闭合/抬起。 | 固定测试场景批量评估，分类视觉/规划/执行/滑落失败。 |
| C08 | sim2real：成像、几何、摩擦接触、驱动、延迟、分布与域随机化。 | 改质量/摩擦/噪声/偏差/延迟；敏感性测试不冒充真机迁移。 |

### D｜数据与模仿学习

| 编号 | 知识内容 | 实验与验收证据 |
|---|---|---|
| D01 | 观测、动作、时间：本体状态、绝对/增量、关节/末端、单位与频率。 | 明确环境接口，同一动作记录与回放对照。 |
| D02 | 示范与 LeRobot：episode、时间对齐、分组划分、归一化、格式版本。 | 读取/回放公开小子集，导出少量自有数据；不先下载全集。 |
| D03 | BC：监督学习观测到动作，标签/损失与闭环执行区别。 | 训练小 BC，独立成功判据评估，不只看训练 loss。 |
| D04 | 复合误差与恢复：执行分布偏移、恢复数据、DAgger 思想。 | 初始偏移/中途扰动，加入恢复示范前后对照。 |
| D05 | 序列模型：RNN/Transformer、token、历史、上下文、延迟和内存。 | 小序列例子；长任务不等于单纯换成 Transformer。 |
| D06 | ACT：动作块、预测/执行步数、时间聚合与训练/推理输入。 | 固定任务跑通，与 BC 用可比数据和评估。 |
| D07 | Diffusion Policy：动作去噪、多模态、预测/执行时域、步数与延迟。 | 同任务小训练/评估，对照成功率与速度。 |
| D08 | 数据与泛化：公开数据集、UMI、第一视角采集、仿真/真机混合。 | 数据卡、未见位置/外观/扰动评估；Ego 未给全名，不猜具体项目。 |

### E｜强化学习与 SAC 深入

| 编号 | 知识内容 | 实验与验收证据 |
|---|---|---|
| E01 | MDP：状态/观测/动作/奖励/转移/折扣/终止；截断不等于终止。 | P0 环境封装，检查一条 transition。 |
| E02 | 回报、V/Q、Bellman、自举与 TD-error。 | 手算回报、目标和误差；检查维度与终止掩码。 |
| E03 | DQN/Double DQN：online/target、选择与评价分离、目标更新。 | 小离散任务核心更新，手算两个目标。 |
| E04 | On/Off-policy：行为策略、目标策略、数据复用、样本与计算效率。 | 追踪数据由谁产生、谁使用，分别统计交互与更新。 |
| E05 | 重要性采样/PPO：分布校正、概率比、裁剪、不同偏差来源。 | 手算换分布期望；对照 PPO ratio 与回放采样校正。 |
| E06 | Replay/PER：优先级、概率、权重、偏差、异常 TD-error。 | 合成采样频率/权重实验；主 SAC 先均匀回放。 |
| E07 | 奖励与 shaping：稀疏/稠密、成功判据、平滑项、势函数与投机。 | 构造错误奖励反例；说明何时保持/改变原任务最优策略。 |
| E08 | 连续/离散/混合动作：参数化、分支分布、共享主干、约束与尺度。 | 移动加夹爪接口，比较语义和组合规模。 |
| E09 | Actor-Critic 与梯度：动作生成、动作评价、重参数化和替代估计。 | 高斯小例子检查梯度，区别路径导数与其他估计器。 |
| E10 | 最大熵与温度：探索权衡、微分熵、目标熵与启发式边界。 | 方差与熵实验；负目标熵不代表负概率。 |
| E11 | SAC Critic：双 Q、min、自举、熵项、mask、detach、target。 | 逐行写一次 Critic 更新，检查形状与梯度归属。 |
| E12 | SAC Actor：重参数化、tanh、log-prob 校正、优化方向。 | 推导与实现 Actor 更新；检查策略与 Critic 参数的梯度。 |
| E13 | Alpha 自适应：熵约束、alpha/log-alpha、符号、detach 与固定温度。 | 人工熵残差检验增减方向，再做固定/自适应对照。 |
| E14 | 完整循环与探索：采样/回放/更新/评估/种子、示范、课程化、干预、安全。 | 可靠基线逐步替换教学实现；记录回报/成功率/Q/Alpha/动作。 |
| E15 | RL/BC/离线 RL：覆盖、分布、价值外推、保守性和恢复。 | 相同任务扰动对照；不能从算法名称保证泛化更好。 |
| E16 | 对照与迁移诊断：多种子、温度/奖励/动作尺度/延迟/随机化。 | 两项小对照、波动与失败解释，不只展示最佳结果。 |

### F｜VLA 与 π 系列

| 编号 | 知识内容 | 实验与验收证据 |
|---|---|---|
| F01 | VLM：图像编码器、语言主干、连接模块、token 与张量维度。 | 带输入输出尺寸的结构图。 |
| F02 | VLM 到 VLA：动作输出、图像/语言/本体状态融合、动作空间和频率。 | 实际 batch 的字段、坐标、单位、尺寸、归一化。 |
| F03 | RT/OpenVLA：任务、数据、动作生成、原版与改进、流行度证据。 | 有版本与日期的对照表，不把题干的流行度假设当事实。 |
| F04 | LoRA/全参数：冻结、低秩增量、参数数目、梯度、rank。 | 小模型插入 LoRA，统计参数和输出变化后再迁移 VLA。 |
| F05 | Flow Matching：噪声到动作的连续生成、扩散/自回归差异。 | 低维生成小实验，理解目标而非背大公式。 |
| F06 | π0：VLM 主干、action expert、信息交互与连续动作块。 | 论文与指定实现的一次前向追踪，标明版本。 |
| F07 | FAST/DCT：序列频域、量化、压缩、还原、训练与推理速度区别。 | 编解码对照 token 数、重建误差和延迟；区分变换与有损步骤。 |
| F08 | π0.5：开放世界、异构任务联合训练、高层语义与低层动作。 | 具体任务的数据/子任务/动作链及证据边界。 |
| F09 | 知识隔离与分层：初版论文、后续方法、实现差异；注意力与梯度不同。 | 标注 token 可见性、梯度流、损失更新范围。 |
| F10 | 本体状态编码：连续投影/分桶、位置、掩码、归一化。 | 读取锁定代码的 preprocessing/forward，给真实尺寸，不凭名称猜。 |
| F11 | π0.6、π*0.6 及后续公开迭代：版本问题、训练/推理改动与证据。 | 学到时核验官方日期、论文、代码和权重，未知细节保留；不把静态计划称为最新榜单。 |
| F12 | VLA+RL：监督初始化、交互、评价、改进、安全和部署分级。 | 公开 checkpoint 输入输出与一项小评估；显存实测后才安排小微调。 |

### G｜C++、数据结构与 Python 工程基础（穿插）

| 编号 | 知识内容 | 实验与验收证据 |
|---|---|---|
| G01 | **C++ 零基础与对象模型**：变量/类型、函数、引用/指针、const、class、构造/析构、栈/堆、生命周期。 | 写最小类与资源对象；能解释值、引用、指针和对象何时创建/销毁。 |
| G02 | **数据结构 I：线性结构与复杂度**。Big-O；array/vector/string；单/双链表；stack、queue、deque；遍历、插入、删除、查找。 | 手写/调用最小例子；比较 O(1)、O(n)；用 stack/queue 解决一个小问题。 |
| G03 | **数据结构 II：哈希、堆、树、图与基础算法**。map/unordered_map/set；heap/priority_queue；binary tree；graph；BFS/DFS；排序、二分搜索；与 A*/RRT 的数据结构对应。 | 用 priority_queue 重看 Open Set；用 unordered_map/set 做访问记录；手写 BFS/DFS 最小例子。 |
| G04 | **Python 数据语义**：赋值、可变/不可变、浅拷贝/深拷贝、list/dict/set、NumPy view/copy、嵌套对象。 | 数据集/episode 示例中验证共享引用、copy 与 deepcopy 的差别。 |
| G05 | **C++ 面经强化与资源管理**：继承、virtual/纯 virtual、override、动态绑定、RAII、unique_ptr/shared_ptr/weak_ptr、循环引用。 | 控制器基类+两个实现；构造并修复 shared_ptr 循环引用。 |
| G06 | **Python 工程与可复现性**：函数对象/闭包/装饰器；计时；Git、配置、日志、种子、断言、测试、环境隔离。 | 写推理计时装饰器；每个项目保存版本、命令、依赖、随机种子和结果。 |

### H｜项目整合与面试验收

| 编号 | 知识内容 | 实验与验收证据 |
|---|---|---|
| H01 | 传统抓取答辩：图像到轨迹全链路及失败边界。 | 10 分钟介绍，现场改一个坐标/安装/筛选参数并预测。 |
| H02 | 学习项目答辩：数据划分、基线、指标、分布、SAC 损失与对照。 | 解释自己的实验，不只复述论文。 |
| H03 | 逐题模拟面试：基础、推导、代码、反例与追问。 | Q001–Q095 逐题评分，低分返回对应单元补学。 |
| H04 | 项目材料：README、视频、个人修改、局限与经历边界。 | 形成可信项目介绍，真机未做不包装真机经验。 |

## 6. 必须修正的易错前提

正式推导在对应单元完成，本节只防止把错误前提写成学习目标。

- 旋转矩阵与位置向量不同；末端在 (1,1) 不代表自身朝向是 45 度。
- 两关节限制独立可控的速度组合，不表示六个速度分量中只能有两个非零；Jacobian 的行数由任务决定。
- 平面二连杆只转第一个关节、保持第二关节为零，仍然共线；不能说随便动一个关节就脱离位置任务奇异性。
- 从伸直状态沿原直线内收可以通过协调两关节实现，但有限关节速度启动时不能指定非零径向瞬时速度；并非必须偏离路径。
- 最大化目标与最小化负目标不是两种相反算法；SAC 要看完整熵正则目标。
- 不使用重参数化不代表不能学习策略，还存在其他梯度估计；区分直接路径求导与替代估计。
- Alpha 不是负动作维数；目标熵启发值与温度参数不同，且约定依分布与实现。
- 重要性采样要说清校正哪种分布；不能概括为 SAC 永远不用任何校正。
- RL 不保证普遍比 BC 泛化好；恢复能力要联系反馈、训练覆盖、恢复数据或重规划。
- DCT 本身与量化/截断不同；FAST 的训练提速不等于任何配置推理都更快。
- π 系列的不同分支与实现不可混称；状态 token 位置、注意力、梯度等必须看指定版本。
- OpenVLA 的流行度需要时间和指标；Ego 未给出全名；未明确的问题不猜答案。

## 7. 资料与环境策略

以下为既定路线的官方/作者资料入口，不表示本次仓库初始化重新逐篇核验了所有技术结论。进入单元时核对实际版本、API 与论文，尤其是“最新模型”“默认行为”“显存要求”。课程覆盖固定，F11 负责公开迭代核验，不追热点扩张整个计划。

| 用途 | 资料入口 | 使用方式 |
|---|---|---|
| MuJoCo | https://mujoco.readthedocs.io/en/stable/python.html ; https://mujoco.readthedocs.io/en/stable/XMLreference.html | 对照实际安装版本，自己的小模型为主。 |
| 机器人模型 | https://github.com/UniversalRobots/Universal_Robots_ROS2_Description ; https://github.com/google-deepmind/mujoco_menagerie | 使用前冻结同一型号、commit 与工具定义。 |
| MoveIt | https://moveit.picknik.ai/main/doc/tutorials/getting_started/getting_started.html | 到 B 阶段再选择经核验的 Ubuntu/ROS 2 组合。 |
| GraspNet | https://github.com/graspnet/graspnet-baseline | 预训练、小样例，旧依赖隔离。 |
| LeRobot | https://github.com/huggingface/lerobot ; https://huggingface.co/docs/lerobot/ | 主数据/训练框架，锁定代码与数据格式。 |
| ACT / Diffusion | https://github.com/tonyzhaozh/act ; https://github.com/real-stanford/diffusion_policy | 作者实现对照，不同时重搭多套主框架。 |
| SAC | https://arxiv.org/abs/1812.05905 ; https://spinningup.openai.com/en/latest/algorithms/sac.html ; https://docs.cleanrl.dev/rl-algorithms/sac/ | 原理、损失与可读实现交叉核对。 |
| PPO / PER / DAgger | https://arxiv.org/abs/1707.06347 ; https://arxiv.org/abs/1511.05952 ; https://arxiv.org/abs/1011.0686 | 对应 E05/E06/D04，先小实验。 |
| VLA / π | https://github.com/Physical-Intelligence/openpi ; https://www.pi.website/ ; https://github.com/openvla/openvla | 发布、开源、权重可用、可运行分别核验。 |
| FAST / 知识隔离 | https://www.pi.website/research/fast ; https://www.pi.website/research/knowledge_insulation | 区分版本与训练/推理机制。 |
| LoRA | https://arxiv.org/abs/2106.09685 | 先在线性层理解和验证。 |
| 轻量采集 | https://umi-gripper.github.io/ ; https://egomimic.github.io/ ; https://machinelearning.apple.com/research/egodex-learning-dexterous-manipulation | 明确命名的方案，不将第一视角视频等同动作标签。 |

每个项目冻结：仓库 URL/commit、系统、Python/依赖、数据格式、机器人型号、坐标/单位与控制频率。Windows 本机做编辑与小仿真，远程 Linux 做适合的训练；图形、网络、设备与显存限制实测，不凭 GPU 型号承诺全部可运行。

## 8. 防止跑偏

每次先读状态卡，报当前单元与本次唯一问题；术语先定义，相关默认值、原点、单位、正方向、继承先解释。提问发现前提缺失就补齐，再回当前主线。

更新只写真实证据；课堂回答、用户完成报告、源码审阅、实测验收分开记。评分规则与提交流程见 [教学约定](TEACHING_RULES.md)。

变更只因用户目标调整、实测兼容阻塞或等价练习需要；记录旧路线、原因、新路线、保留验收。第一轮不新增 Isaac Sim 独立路线、人形、多机器人、多指灵巧手、大 VLA 从零训练或大数据全集下载。相关拓展放在完成主线之后。
