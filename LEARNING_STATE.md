# 学习进度与下一次接续卡

> 本文件是当前进度的唯一权威记录。总计划见 MASTER_PLAN.md。自 v1.2 起，**只用 A01–H04 作为正式课程编号**；旧对话中的 M01–M08 只作为历史实操记录，不再决定下一课。

更新日期：2026-09-16。
课程基线：v1.0，72 单元 / 95 题；仓库执行修订：v1.2。
当前阶段：A——二连杆与 MuJoCo 基础闭环。
当前项目：P0——基础练习台。
当前课程：**A07——Jacobian、奇异性与阻尼**。

## 1. 已完成到哪里

### A01 环境与已有进度核验
- MuJoCo 3.13.0 已安装并可用；Viewer 已运行。
- 最小 MJCF 已建立。
- 证据：用户报告完成。

### A02 刚体、形状与坐标系
- 已学习 body / geom、body frame、局部坐标、父子关系。
- 已理解 geom 的几何长度不会自动决定下一个 body/joint 的连接位置。
- 已讨论 body 原点由建模者定义，而不是由杆本身自动决定。
- 仍可在后续真正搭二连杆时继续巩固，但不作为当前主课号。

### A03 关节位置、方向与默认值
- 已学习 hinge、joint pos、axis、局部轴和 world/body frame。
- 已实际尝试 axis=`0 1 0`、`1 0 0`、`1 1 0`。
- 已明确 joint pos 是关节轴经过的点，axis 是方向向量。
- 已明确不能省略影响理解的默认值、单位、参考系和继承前提。

### A04 仿真状态与 FK
- 已学习 qpos/qvel、mj_forward/mj_step、直接改状态与推进动力学的区别。
- 已做 45° 初始角度的重力摆动实验。
- 已在理论课完成二连杆 FK 公式、坐标变换、Transformation Matrix 的推导与纸笔计算。
- 总计划里的“二连杆 FK 与仿真末端坐标数值对照”仍可在后续二连杆模型完成后补证据；这不阻塞 A07。

### A05 驱动命令与直接改状态
- 已学习 joint / actuator / qpos / qvel / ctrl 的区别。
- 已完成 motor actuator 实验：正负 ctrl 改变驱动方向。
- 已完成 position actuator 实验：ctrl 作为目标关节位置；已说明 position 默认不能未经参数检查直接称完整 PD。
- 已理解 motor 的 ctrl 不是目标角度；gear / transmission 会影响最终广义力/力矩。
- 证据：课堂问答 + 用户报告完成实验。

### A06 二连杆 IK
- 已区分位置 IK 与完整位姿 IK。
- 已理解解析 IK 的几何关系、`theta2=±arccos(...)` 两分支、0/1/2 个几何解。
- 已理解可达范围和边界解。
- 已学习数值 IK：从初值迭代、任务空间误差、初值/局部 Jacobian/步长/阻尼对收敛的影响。
- 已运行 IK→FK 数学验证：`(1,1)` 两解、`(2,0)` 两分支合并为一个几何解、`(3,0)` 无解。
- 证据：课堂问答 + 用户报告运行；详见 `learning_logs/2026-09-16_A06_ik.md`。

## 2. 当前唯一任务：A07 Jacobian、奇异性与阻尼

知识目标：
1. 明确 Jacobian 不是姿态矩阵，而是“关节小变化/速度 → 末端小变化/速度”的局部线性映射。
2. 真正理解 Jacobian 的行和列分别代表什么；矩阵维度由任务维度和关节自由度决定。
3. 用二连杆的 FK 求偏导得到位置 Jacobian，并用有限差分验证。
4. 对照弯曲、近伸直、完全伸直构型，观察哪些末端瞬时速度方向逐渐丢失。
5. 区分“机器人还能离开这个构型”和“某个指定瞬时末端速度在该构型下不能实现”。
6. 理解伪逆为什么在近奇异处产生很大的关节更新。
7. 学习阻尼最小二乘 / damped pseudoinverse 的直觉：允许一点任务误差，换取更有限、更稳定的关节更新。
8. 做一个最小数值实验，不只背 `rank(J)<...` 或 `det(J)=0`。

教学顺序：先用二维二连杆和具体数字，不先上 SVD 大段数学；需要新术语时先定义。之前关于奇异点的讨论作为直觉基础，但本课要用数值实验重新验证，避免把特殊结论讲成普遍结论。

## 3. A 阶段路线

| 正式课号 | 内容 | 当前状态 |
|---|---|---|
| A01 | 环境与已有进度核验 | 已完成首轮 |
| A02 | 刚体、形状与坐标系 | 已完成首轮，后续二连杆实操继续巩固 |
| A03 | joint 位置、方向与默认值 | 已完成首轮 |
| A04 | 状态、FK、仿真推进 | 已完成核心理论与单杆实验；二连杆数值对照待补证据 |
| A05 | actuator / ctrl / 直接改状态 | 已完成首轮实验 |
| A06 | 二连杆 IK | **已完成首轮** |
| **A07** | **Jacobian、奇异性与阻尼** | **当前课程** |
| A08 | 最小控制与验收：PD、周期、限位、扰动 | A07 后 |

完成 A08 后进入 B01。

## 4. 旧 M01–M08 如何处理

旧对话的 M 课号只作为历史实操证据：环境、单杆、qpos/qvel、Actuator 等已经产生了真实学习价值；其中“搭二连杆、双关节控制、FK 对照”等未完成内容，将按 A04/A07/A08 的需要穿插完成。

原则：总计划决定学习顺序，项目实验服务知识单元，而不是反过来让旧项目课表决定课程。

## 5. 现有本地文件

用户已报告本地存在：
- `01_MUJOCO_BASICS/models/two_link.xml`（当前内容仍是单杆阶段）
- `scripts/set_qpos.py`
- `scripts/animate_qpos.py`
- `scripts/gravity_sim.py`
- Actuator 相关 motor / position 实验文件（具体文件名未统一记录）
- A06 的 IK/FK 数学验证脚本（本地运行，尚未提交仓库）

这些代码尚未完整提交仓库；不伪造源码。后续需要 A07 数值验证时，优先做独立数学脚本；需要仿真时再扩展现有项目。

## 6. 接续规则

每次开始先读本文件和 TEACHING_RULES.md。只按正式 A/B/C... 课号报进度。课后更新：实际完成、仍不理解、证据等级、下一正式单元。路线变化写 CHANGELOG.md。
