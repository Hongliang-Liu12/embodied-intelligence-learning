# 学习进度与下一次接续卡

> 本文件是当前进度的唯一权威记录。总计划见 MASTER_PLAN.md。自 v1.2 起，**只用 A01–H04 作为正式课程编号**；旧对话中的 M01–M08 只作为历史实操记录，不再决定下一课。

更新日期：2026-09-16。
课程基线：v1.0，72 单元 / 95 题；仓库执行修订：v1.2。
当前阶段：A——二连杆与 MuJoCo 基础闭环。
当前项目：P0——基础练习台。
当前课程：**A08——最小控制与验收：PD、控制周期、限位、记录与扰动**。

## 1. 已完成到哪里

### A01 环境与已有进度核验
- MuJoCo 3.13.0 已安装并可用；Viewer 已运行。
- 最小 MJCF 已建立。
- 证据：用户报告完成。

### A02 刚体、形状与坐标系
- 已学习 body / geom、body frame、局部坐标、父子关系。
- 已理解 geom 的几何长度不会自动决定下一个 body/joint 的连接位置。
- 已讨论 body 原点由建模者定义，而不是由杆本身自动决定。

### A03 关节位置、方向与默认值
- 已学习 hinge、joint pos、axis、局部轴和 world/body frame。
- 已实际尝试 axis=`0 1 0`、`1 0 0`、`1 1 0`。
- 已明确 joint pos 是关节轴经过的点，axis 是方向向量。
- 已明确不能省略影响理解的默认值、单位、参考系和继承前提。

### A04 仿真状态与 FK
- 已学习 qpos/qvel、mj_forward/mj_step、直接改状态与推进动力学的区别。
- 已做 45° 初始角度的重力摆动实验。
- 已完成二连杆 FK 公式、坐标变换、Transformation Matrix 的推导与纸笔计算。
- 二连杆 FK 与 MuJoCo 末端坐标数值对照仍可在后续二连杆模型完成后补证据，不阻塞 A08。

### A05 驱动命令与直接改状态
- 已学习 joint / actuator / qpos / qvel / ctrl 的区别。
- 已完成 motor actuator 实验：正负 ctrl 改变驱动方向。
- 已完成 position actuator 实验：ctrl 作为目标关节位置；已说明 position 默认不能未经参数检查直接称完整 PD。
- 已理解 motor 的 ctrl 不是目标角度；gear / transmission 会影响最终广义力/力矩。

### A06 二连杆 IK
- 已区分位置 IK 与完整位姿 IK。
- 已理解解析 IK 的几何关系、`theta2=±arccos(...)` 两分支、0/1/2 个几何解。
- 已理解可达范围和边界解。
- 已学习数值 IK：从初值迭代、任务空间误差、初值/局部 Jacobian/步长/阻尼对收敛的影响。
- 已运行独立 Python 数学验证：`(1,1)` 两解、`(2,0)` 两分支合并为一个几何解、`(3,0)` 无解。
- A06 不强制 MuJoCo 动态执行；MuJoCo 联动后置到控制/轨迹执行需要时。

### A07 Jacobian、奇异性与阻尼
- 已明确 Jacobian 是关节小变化/速度到末端小变化/速度的局部线性映射，不是姿态矩阵。
- 已理解 Jacobian 行 = 任务空间运动量，列 = 关节变量；二维二连杆位置任务为 2×2，若再加入末端朝向则为 3×2。
- 已从 FK 对关节变量求偏导得到二连杆位置 Jacobian。
- 已用小角度变化理解 `Δx ≈ JΔq`，并区分局部线性近似与大角度运动。
- 已理解 rank 可直观理解为独立方向数量；列向量线性相关时独立末端运动方向减少。
- 已比较正常、近奇异、完全奇异：近奇异仍可理论实现弱方向，但可能要求很大关节速度；完全奇异时某些指定瞬时末端速度没有精确解。
- 已理解伪逆在近奇异处为什么会放大关节速度：大关节速度的主要分量相互抵消，只留下很弱的目标方向分量。
- 已理解阻尼伪逆 / damped least squares 的核心折中：允许一些末端误差，换取更有限、更稳定的关节更新。
- 已用正常/近奇异/完全奇异的最小矩阵例子进行数值逻辑验收。

## 2. 当前唯一任务：A08 最小控制与验收

知识目标：
1. 区分开环与闭环控制；明确“目标、测量、误差、控制器、执行器、系统响应”这条链。
2. 理解 P、D、I 各自作用；本阶段重点 PD，PID 的 I 项只在需要消除稳态误差时引入。
3. 解释关节位置 PD 的基本形式：`τ = Kp(q_des-q) + Kd(qdot_des-qdot)`，以及每一项的物理直觉和符号。
4. 理解控制周期 / timestep：为什么同一增益在不同更新频率下表现可能不同；仿真步长与控制更新频率可以不同。
5. 理解关节限位、速度/力矩限幅、饱和与积分饱和的基本风险。
6. 学会记录目标角、实际角、误差、qvel、ctrl/torque，并用曲线判断超调、振荡、收敛、稳态误差。
7. 做一次小扰动恢复：系统被推离目标后，闭环是否能拉回；区分“能回目标”和“恢复快且不过度振荡”。
8. 用现有 MuJoCo 单关节先做最小 PD 实验；如需要二连杆再扩展，不为形式强行增加复杂度。

教学顺序：先从单关节位置控制和生活直觉讲闭环/P/D，再给最小公式；不直接上完整控制理论。每个新参数说明单位、参考方向、执行器类型和默认/显式设置。

## 3. A 阶段路线

| 正式课号 | 内容 | 当前状态 |
|---|---|---|
| A01 | 环境与已有进度核验 | 已完成首轮 |
| A02 | 刚体、形状与坐标系 | 已完成首轮，后续实操继续巩固 |
| A03 | joint 位置、方向与默认值 | 已完成首轮 |
| A04 | 状态、FK、仿真推进 | 已完成核心理论与单杆实验；二连杆数值对照待补证据 |
| A05 | actuator / ctrl / 直接改状态 | 已完成首轮实验 |
| A06 | 二连杆 IK | 已完成首轮；数值验证已完成 |
| A07 | Jacobian、奇异性与阻尼 | **已完成首轮** |
| **A08** | **最小控制与验收：PD、周期、限位、扰动** | **当前课程** |

完成 A08 后进入 B01。

## 4. 现有本地文件与实验边界

用户已报告本地存在：
- `01_MUJOCO_BASICS/models/two_link.xml`（当前内容仍是单杆阶段）
- `scripts/set_qpos.py`
- `scripts/animate_qpos.py`
- `scripts/gravity_sim.py`
- Actuator 相关 motor / position 实验文件
- A06 IK/FK 数学验证脚本

上述本地代码尚未完整提交仓库，不伪造源码。A08 优先复用已有单关节模型和 actuator 实验，在最小复杂度下完成 PD/记录/扰动理解。

## 5. 接续规则

每次开始先读本文件和 TEACHING_RULES.md。只按正式 A/B/C... 课号报进度。课后更新：实际完成、仍不理解、证据等级、下一正式单元。路线变化写 CHANGELOG.md。
